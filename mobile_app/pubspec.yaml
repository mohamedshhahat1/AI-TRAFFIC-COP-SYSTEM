name: ai_traffic_cop
description: AI Traffic Cop System - Mobile App with Event Bus & Monitoring

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.1.0
  fl_chart: ^0.65.0
  intl: ^0.18.0
  web_socket_channel: ^2.4.0
  shared_preferences: ^2.2.0
  flutter_local_notifications: ^16.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
